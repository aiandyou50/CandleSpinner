# 제미나이 제안 아키텍처 분석: '서명된 요청' 방식

**작성일**: 2025-11-02  
**제안**: 메시지 서명 기반 인출 시스템  
**결론**: ⚠️ 이론적으로 가능하나 실용적 문제 다수

---

## 🎯 제안 요약

### 핵심 아이디어
```
1. 사용자: 인출 요청 메시지 서명 (수수료 없음)
2. 서버: 서명 검증 + 게임 지갑으로 토큰 전송
3. 비용: 게임이 네트워크 Fee 부담
```

### 아키텍처 흐름
```
[사용자]
  ↓ 메시지 서명 (무료)
[Cloudflare Workers]
  ↓ 1. 서명 검증 (ton-crypto)
  ↓ 2. 크레딧 확인 (KV)
  ↓ 3. 운영 지갑으로 트랜잭션 생성
  ↓ 4. 블록체인 전송
[TON 블록체인]
  ↓ 게임 → 사용자 (CSPIN)
[사용자]
```

---

## ✅ 이론적 가능성

### 1. 메시지 서명 (프론트엔드)
```typescript
// React 앱
import { useTonConnectUI } from '@tonconnect/ui-react';

const [tonConnectUI] = useTonConnectUI();

async function handleWithdraw(amount: number) {
  // 1. 인출 요청 메시지 생성
  const withdrawRequest = {
    action: 'withdraw',
    amount,
    userAddress: walletAddress,
    timestamp: Date.now(),
    nonce: Math.random().toString(36)  // 리플레이 공격 방지
  };
  
  // 2. 메시지를 문자열로 변환
  const message = JSON.stringify(withdrawRequest);
  
  // 3. TON Connect로 메시지 서명 요청
  const signature = await tonConnectUI.signMessage({
    message,
    // ✅ 사용자는 "메시지 서명" 팝업 보게 됨 (수수료 없음)
  });
  
  // 4. 서명된 메시지를 백엔드로 전송
  const response = await fetch('/api/withdraw-signed', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      request: withdrawRequest,
      signature: signature.signature,
      publicKey: signature.publicKey
    })
  });
}
```

### 2. 서명 검증 (백엔드 - Cloudflare Workers)
```typescript
// src/index.ts
import { verifySignature } from 'ton-crypto';  // ❌ 문제 1: 라이브러리 제약

async function handleWithdrawSigned(request: Request, env: Env) {
  const body = await request.json();
  const { request: withdrawRequest, signature, publicKey } = body;
  
  // 1. 서명 검증
  const message = JSON.stringify(withdrawRequest);
  const isValid = await verifySignature(
    Buffer.from(message),
    signature,
    publicKey
  );
  
  if (!isValid) {
    return new Response('Invalid signature', { status: 401 });
  }
  
  // 2. 타임스탬프 검증 (리플레이 공격 방지)
  const age = Date.now() - withdrawRequest.timestamp;
  if (age > 60000) {  // 1분 초과
    return new Response('Request expired', { status: 401 });
  }
  
  // 3. 크레딧 확인
  const creditKey = `credit:${withdrawRequest.userAddress}`;
  const credit = await env.CREDIT_KV.get(creditKey, 'json');
  if (credit.amount < withdrawRequest.amount) {
    return new Response('Insufficient credit', { status: 400 });
  }
  
  // 4. 운영 지갑으로 트랜잭션 생성 (❌ 문제 2: window is not defined)
  const mnemonic = env.GAME_WALLET_MNEMONIC.split(' ');
  const keyPair = await mnemonicToPrivateKey(mnemonic);  // ❌ 실패!
  
  // 5. 토큰 전송
  // ...
}
```

---

## ❌ 실제 문제점

### 문제 1: ton-crypto 라이브러리 제약

**제미나이 제안**: `ton-crypto`로 서명 검증

**현실**:
```typescript
import { verifySignature } from 'ton-crypto';

// Cloudflare Workers에서 실행하면:
// ❌ Error: "window is not defined"
// ❌ Error: "crypto is not defined"
```

**이유**:
- `ton-crypto`도 `@ton/crypto`처럼 브라우저 API 의존
- Cloudflare Workers는 V8 isolate 환경
- Node.js 환경과도 다름

**대안**:
```typescript
// 1. Web Crypto API 사용 (Workers 지원)
const isValid = await crypto.subtle.verify(
  'RSASSA-PKCS1-v1_5',
  publicKey,
  signature,
  message
);

// 2. 또는 TON 특화 검증 로직 직접 구현
// (매우 복잡, 비추천)
```

### 문제 2: 트랜잭션 생성 여전히 불가능

**제미나이 제안**: Workers에서 운영 지갑으로 트랜잭션 생성

**현실**:
```typescript
// Cloudflare Workers
import { mnemonicToPrivateKey } from '@ton/crypto';
import { WalletContractV5R1 } from '@ton/ton';

const keyPair = await mnemonicToPrivateKey(mnemonic);
// ❌ Error: "window is not defined"

const wallet = WalletContractV5R1.create({ ... });
// ❌ Error: "window is not defined"
```

**결론**: 
- ✅ 서명 검증: 가능 (Web Crypto API)
- ❌ 트랜잭션 생성: **여전히 불가능** (@ton/ton 제약)

---

## 🔧 실제 구현 가능 여부

### Case 1: Cloudflare Workers만 사용
```
❌ 불가능

이유:
- 서명 검증: ✅ 가능 (Web Crypto API)
- 트랜잭션 생성: ❌ 불가능 (@ton/ton 제약)
→ 핵심 기능 구현 불가
```

### Case 2: Node.js 서버 추가
```
✅ 가능

구조:
[Cloudflare Workers]
  ↓ 서명 검증 (Web Crypto API)
  ↓ 크레딧 확인
  ↓ 검증 통과 시 Node.js 서버 호출
[Node.js 서버]
  ↓ 트랜잭션 생성 (@ton/ton 사용 가능)
  ↓ 블록체인 전송
[TON 블록체인]

비용: $20~50/월
```

### Case 3: 완전 프론트엔드 + 백엔드 검증
```
✅ 현실적

구조:
[프론트엔드]
  ↓ 메시지 서명
  ↓ 백엔드 검증 요청
[Cloudflare Workers]
  ↓ 서명 검증
  ↓ 크레딧 차감
  ↓ 대기열 추가
[관리자]
  ↓ TON Connect로 수동 처리
[TON 블록체인]

비용: $0 (현재 구현)
```

---

## 💰 비용 문제 (중요!)

### 제미나이 지적: "게임 유저가 Fee 지불"은 불가능

**맞습니다!** 🎯

**이유**:
```
토큰을 '보내는' 주체 = 네트워크 Fee 지불자

인출: 게임 → 사용자
      ↑
      게임이 Fee 지불해야 함!
```

**비용 계산**:
```
인출 1건: 0.2 TON
일 10건: 2 TON
월 300건: 60 TON

TON 가격: $2.50 가정
월 비용: 60 × $2.50 = $150
```

**현실적 문제**:
```
게임 초기 단계:
- 수익 없음
- 인출 요청 적음 (일 0~3건)
- 월 비용: $0~$25

$150/월은 과도한 부담!
```

---

## 🎯 권장 솔루션

### MVP 단계 (현재 ~ 3개월)
**수동 인출 시스템** (이미 구현됨)

```typescript
// 프론트엔드: 메시지 서명
const signature = await tonConnectUI.signMessage({ message });

// 백엔드: 서명 검증 + 크레딧 차감
const isValid = await crypto.subtle.verify(...);
if (isValid) {
  await deductCredit(user, amount);
  await addToQueue(withdrawalRequest);
}

// 관리자: 수동 처리 (하루 1회)
// - 배치로 처리하여 수수료 절감
// - 비용 통제 가능
```

**장점**:
- ✅ 인프라 비용 $0
- ✅ Cloudflare Workers만으로 구현 가능
- ✅ 메시지 서명으로 보안 강화 가능
- ✅ 비용 통제 가능

**단점**:
- ❌ 처리 지연 (12~24시간)
- ❌ 수동 작업 필요

### 성장 단계 (3개월 ~ 6개월)
**Node.js 자동 인출**

```typescript
// Cloudflare Workers: 메시지 서명 검증
const isValid = await crypto.subtle.verify(...);

// Node.js 서버: 트랜잭션 생성
const keyPair = await mnemonicToPrivateKey(mnemonic);  // ✅ 가능!
const wallet = WalletContractV5R1.create({ ... });
await wallet.sendTransfer({ ... });
```

**비용**: $50/월 (서버) + 네트워크 Fee

### 메인넷 단계 (6개월+)
**스마트 컨트랙트**

```tact
contract WithdrawManager {
  receive(msg: WithdrawRequest) {
    // 온체인 검증
    // 자동 토큰 전송
  }
}
```

**비용**: ~5 TON (배포) + 사용자 가스비

---

## 📋 제미나이 제안 평가

### ✅ 좋은 아이디어
1. **메시지 서명 인증**: 보안 강화
2. **리플레이 공격 방지**: timestamp + nonce
3. **비용 통제**: 게임이 Fee 부담 인식

### ❌ 간과한 문제
1. **Cloudflare Workers 제약**: @ton/ton 사용 불가
2. **비용 현실성**: MVP에서 $150/월은 과도
3. **구현 복잡도**: Web Crypto API 직접 구현 필요

### ⚠️ 부분 채택 가능
```typescript
// 1단계: 메시지 서명 (✅ 채택)
const signature = await tonConnectUI.signMessage({ ... });

// 2단계: 서명 검증 (✅ 채택 - Web Crypto API)
const isValid = await crypto.subtle.verify(...);

// 3단계: 크레딧 차감 (✅ 채택)
await deductCredit(user, amount);

// 4단계: 대기열 추가 (✅ 현실적 대안)
await addToQueue(withdrawal);

// 5단계: 수동 처리 (✅ MVP)
// 또는 Node.js 서버 (✅ 성장기)
```

---

## 🔧 실제 구현 예시 (하이브리드)

### 프론트엔드: 메시지 서명
```typescript
// Withdraw.tsx
async function handleWithdraw(amount: number) {
  const withdrawRequest = {
    action: 'withdraw',
    amount,
    userAddress: walletAddress,
    timestamp: Date.now(),
    nonce: crypto.randomUUID()
  };
  
  // TON Connect 메시지 서명
  const signature = await tonConnectUI.signMessage({
    message: JSON.stringify(withdrawRequest)
  });
  
  // 백엔드로 전송
  const response = await fetch('/api/withdraw-signed', {
    method: 'POST',
    body: JSON.stringify({
      request: withdrawRequest,
      signature: signature.signature,
      publicKey: signature.publicKey
    })
  });
}
```

### 백엔드: 서명 검증 + 대기열
```typescript
// src/index.ts
async function handleWithdrawSigned(request: Request, env: Env) {
  const body = await request.json();
  const { request: withdrawRequest, signature, publicKey } = body;
  
  // 1. 서명 검증 (Web Crypto API)
  const encoder = new TextEncoder();
  const message = encoder.encode(JSON.stringify(withdrawRequest));
  
  const publicKeyData = await crypto.subtle.importKey(
    'raw',
    Buffer.from(publicKey, 'hex'),
    { name: 'ECDSA', namedCurve: 'P-256' },
    false,
    ['verify']
  );
  
  const isValid = await crypto.subtle.verify(
    { name: 'ECDSA', hash: 'SHA-256' },
    publicKeyData,
    Buffer.from(signature, 'hex'),
    message
  );
  
  if (!isValid) {
    return new Response('Invalid signature', { status: 401 });
  }
  
  // 2. 타임스탬프 검증
  const age = Date.now() - withdrawRequest.timestamp;
  if (age > 60000) {
    return new Response('Request expired', { status: 401 });
  }
  
  // 3. 크레딧 확인 및 차감
  const creditKey = `credit:${withdrawRequest.userAddress}`;
  const credit = await env.CREDIT_KV.get(creditKey, 'json');
  
  if (credit.amount < withdrawRequest.amount) {
    return new Response('Insufficient credit', { status: 400 });
  }
  
  await env.CREDIT_KV.put(creditKey, JSON.stringify({
    amount: credit.amount - withdrawRequest.amount
  }));
  
  // 4. 대기열 추가 (수동 처리)
  const withdrawalId = `withdraw_${Date.now()}`;
  await env.CREDIT_KV.put(`withdrawal:${withdrawalId}`, JSON.stringify({
    ...withdrawRequest,
    signature,
    publicKey,
    status: 'pending'
  }));
  
  return new Response(JSON.stringify({
    success: true,
    withdrawalId,
    message: '인출 요청이 접수되었습니다 (12~24시간 처리)'
  }));
}
```

---

## 📊 최종 결론

### 제미나이 제안 평가: ⭐⭐⭐☆☆ (3/5)

**장점**:
- ✅ 보안 강화 (메시지 서명)
- ✅ 비용 인식 (게임이 Fee 부담)
- ✅ 이론적 정확성

**단점**:
- ❌ Cloudflare Workers 제약 미고려
- ❌ MVP 비용 현실성 부족
- ❌ 구현 복잡도 과소평가

### 권장 방향

| 단계 | 방식 | 비용 | 서명 검증 | 트랜잭션 생성 |
|------|------|------|----------|-------------|
| **MVP (현재)** | 수동 인출 | $0 | ✅ 가능 | ⏰ 수동 |
| **성장기 (3개월)** | Node.js 자동 | $50/월 | ✅ 가능 | ✅ 자동 |
| **메인넷 (6개월)** | 스마트 컨트랙트 | ~5 TON | ✅ 온체인 | ✅ 온체인 |

### 즉시 구현 가능 여부

**메시지 서명 + 검증 (보안 강화)**:
- ✅ **가능** (Web Crypto API)
- ✅ 현재 시스템에 추가 가능
- ✅ 수동 인출과 병행 가능

**완전 자동 인출**:
- ❌ **불가능** (Cloudflare Workers 제약)
- ✅ Node.js 서버 필요
- ⏰ 3개월 후 검토

---

## 🎯 다음 단계 제안

### 옵션 1: 현재 시스템 유지 (권장)
```
✅ 수동 인출 시스템
✅ 비용 $0
✅ MVP에 충분
```

### 옵션 2: 보안 강화 (메시지 서명 추가)
```
✅ 메시지 서명 인증
✅ Web Crypto API 검증
✅ 현재 시스템에 추가
⏰ 구현 시간: 1~2일
```

### 옵션 3: 완전 자동화 (성장기)
```
✅ Node.js 서버 추가
✅ 자동 인출
❌ 비용: $50/월 + 네트워크 Fee
⏰ 구현 시간: 1주
```

어떤 방향으로 진행하시겠습니까?
