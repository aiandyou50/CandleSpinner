# dependencies
/node_modules
/contracts/node_modules
contracts/node_modules
**/node_modules

# local env files
.env
.env.local
.env.production.local
.env.*.local

# build output
/dist

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# misc
.DS_Store

# TON docs (downloaded for analysis, not to be committed)
ton-docs/

# Jetton token contract (downloaded for analysis, not to be committed)
token-contract/

# Latest Jetton contract (downloaded for analysis, not to be committed)
jetton-contract/

# TON Connect SDK (downloaded for analysis, not to be committed)
sdk/

# Telegram TT SDK (cloned for analysis, not to be committed)
temp_telegram_tt/

# Security - Never commit private keys or sensitive data
wallet-tools/.env*
*.private
*.secret
*.key
SECURITY.md.local

# Terminal history files
.bash_history
.zsh_history
.ps_history

# IDE
.idea/
.vscode/settings.json

