# CandleSpinner 슬롯머신 구현 계획서

**작성일**: 2025-11-04  
**버전**: 1.0  
**프로젝트**: CandleSpinner Slot Machine Game

---

## 목차

1. [프로젝트 개요](#1-프로젝트-개요)
2. [기술 스택 분석](#2-기술-스택-분석)
3. [아키텍처 설계](#3-아키텍처-설계)
4. [구현 단계](#4-구현-단계)
5. [파일 구조](#5-파일-구조)
6. [개발 일정](#6-개발-일정)

---

## 1. 프로젝트 개요

### 1.1 프로젝트 목표

**기존 시스템**: MVP v2 - 입금/인출/관리자 대시보드
**추가 기능**: 3-Reel 슬롯머신 미니게임

### 1.2 핵심 요구사항

✅ **게임 규칙**:
- 3-릴 슬롯머신 (7개 이모지 심볼)
- 독창적 당첨금 계산: 각 릴 개별 합산
- 잭팟 시스템: 3개 동일 심볼 → x100 배수
- 더블업 미니게임: 50% 확률, 1회 제한

✅ **기술 요구사항**:
- Provably Fair 알고리즘 (공정성 검증)
- RTP 95% 유지
- 반응형 디자인 (모바일/태블릿/데스크톱)
- 성능: 스핀 1초 이내, 1,000 동시 접속

✅ **UI/UX**:
- 새로운 디자인 시스템 (보라색 테마)
- 희귀도별 심볼 시각화
- 잭팟 비디오 재생 (`jackpot_video.mp4`)
- 부드러운 애니메이션 효과

---

## 2. 기술 스택 분석

### 2.1 현재 프로젝트 구조

```json
{
  "프론트엔드": {
    "프레임워크": "React 18.3.1",
    "라우팅": "React Router 7.9.5",
    "스타일링": "Tailwind CSS 3.4.14",
    "타입": "TypeScript 5.6.3",
    "빌드": "Vite 5.4.10"
  },
  "백엔드": {
    "플랫폼": "Cloudflare Workers",
    "런타임": "V8 Isolate",
    "스토리지": "KV Namespace",
    "API": "Hono (예상)"
  },
  "블록체인": {
    "네트워크": "TON",
    "SDK": "@ton/ton 15.0.0",
    "지갑": "TON Connect UI"
  }
}
```

### 2.2 추가 필요 라이브러리

| 라이브러리 | 용도 | 우선순위 |
|------------|------|---------|
| `crypto` (Web Crypto API) | Provably Fair 해시 생성 | 필수 |
| `framer-motion` | 고급 애니메이션 | 권장 |
| `react-confetti` | 잭팟 효과 | 선택 |
| `howler.js` | 사운드 효과 | 선택 |

---

## 3. 아키텍처 설계

### 3.1 시스템 구조

```
┌─────────────────────────────────────────────────────┐
│                   사용자 브라우저                   │
│  ┌─────────────────────────────────────────────┐  │
│  │          React 프론트엔드                   │  │
│  │  ┌──────────────┬──────────────────────┐  │  │
│  │  │ SlotMachine  │  DoubleUpModal       │  │  │
│  │  │ Component    │  Component           │  │  │
│  │  └──────────────┴──────────────────────┘  │  │
│  │             │                               │  │
│  │             ▼                               │  │
│  │  ┌──────────────────────────────────────┐  │  │
│  │  │      API Client Layer                │  │  │
│  │  │  /api/slot/spin                      │  │  │
│  │  │  /api/slot/doubleup                  │  │  │
│  │  │  /api/slot/history                   │  │  │
│  │  └──────────────────────────────────────┘  │  │
│  └─────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────┘
                        │
                        ▼ HTTPS
┌─────────────────────────────────────────────────────┐
│            Cloudflare Workers (Edge)                │
│  ┌─────────────────────────────────────────────┐  │
│  │         Slot Game Engine                    │  │
│  │  ┌──────────────┬──────────────────────┐  │  │
│  │  │ Provably     │  RTP Calculator       │  │  │
│  │  │ Fair Logic   │  (95% 유지)          │  │  │
│  │  └──────────────┴──────────────────────┘  │  │
│  │             │                               │  │
│  │             ▼                               │  │
│  │  ┌──────────────────────────────────────┐  │  │
│  │  │      KV Storage                      │  │  │
│  │  │  - game_results                      │  │  │
│  │  │  - provably_fair_seeds               │  │  │
│  │  │  - daily_rtp_stats                   │  │  │
│  │  └──────────────────────────────────────┘  │  │
│  └─────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────┘
```

### 3.2 데이터 흐름

#### 3.2.1 슬롯 스핀 플로우

```
1. [사용자] 베팅 금액 선택 (10~1000 CSPIN)
   ↓
2. [프론트엔드] 클라이언트 시드 생성 (UUID)
   ↓
3. [API] POST /api/slot/spin
   - 요청: { walletAddress, betAmount, clientSeed }
   ↓
4. [서버] 크레딧 차감 확인
   ↓
5. [서버] Provably Fair 결과 생성
   - 서버 시드 + 클라이언트 시드 + 논스 → HMAC-SHA256
   - 해시 → 3개 릴 심볼 결정
   ↓
6. [서버] 당첨금 계산
   - 각 릴의 배당률 합산
   - 잭팟 체크 (3개 동일 심볼)
   ↓
7. [서버] 크레딧 업데이트 (당첨금 추가)
   ↓
8. [서버] 게임 기록 저장 (KV)
   ↓
9. [응답] { result, winAmount, isJackpot, serverSeedHash, nonce }
   ↓
10. [프론트엔드] 애니메이션 재생
    - 스핀 효과 (2초)
    - 결과 표시
    - 당첨 시: 파티클 효과
    - 잭팟 시: 비디오 재생
   ↓
11. [프론트엔드] 당첨금 > 0 → 더블업 팝업
```

#### 3.2.2 더블업 플로우

```
1. [사용자] 빨강/파랑 버튼 선택
   ↓
2. [API] POST /api/slot/doubleup
   - 요청: { walletAddress, choice, currentWin, gameId }
   ↓
3. [서버] 게임 ID 검증 (중복 방지)
   ↓
4. [서버] 난수 생성 (0 또는 1, 50% 확률)
   ↓
5. [서버] 결과 계산
   - 성공: currentWin × 2
   - 실패: 0
   ↓
6. [서버] 크레딧 업데이트
   ↓
7. [서버] 게임 ID 무효화 (재사용 방지)
   ↓
8. [응답] { success, finalAmount }
   ↓
9. [프론트엔드] 결과 애니메이션 (3초 후 자동 닫힘)
```

---

## 4. 구현 단계

### Phase 1: 백엔드 게임 엔진 (우선순위: 최고)

#### 4.1 Provably Fair 시스템

**파일**: `functions/api/slot/provably-fair.ts`

```typescript
import crypto from 'crypto';

interface ProvablyFairResult {
  serverSeed: string;
  serverSeedHash: string;
  clientSeed: string;
  nonce: number;
  hash: string;
  reelResults: string[][];
}

export function generateServerSeed(): string {
  return crypto.randomBytes(32).toString('hex');
}

export function hashServerSeed(serverSeed: string): string {
  return crypto.createHash('sha256').update(serverSeed).digest('hex');
}

export function generateReelResults(
  serverSeed: string,
  clientSeed: string,
  nonce: number
): string[][] {
  // HMAC-SHA256 해시 생성
  const hash = crypto
    .createHmac('sha256', serverSeed)
    .update(`${clientSeed}:${nonce}`)
    .digest('hex');
  
  // 해시를 3개 릴의 심볼로 변환
  const symbols = ['⭐', '🪐', '☄️', '🚀', '👽', '💎', '👑'];
  const probabilities = [35, 25, 15, 10, 7, 5, 3]; // 누적 확률
  
  const reels: string[][] = [];
  
  for (let reelIndex = 0; reelIndex < 3; reelIndex++) {
    const hashSlice = hash.slice(reelIndex * 10, (reelIndex + 1) * 10);
    const randomValue = parseInt(hashSlice, 16) % 100; // 0-99
    
    // 확률 테이블에서 심볼 선택
    let cumulativeProb = 0;
    let selectedSymbol = symbols[0];
    
    for (let i = 0; i < symbols.length; i++) {
      cumulativeProb += probabilities[i];
      if (randomValue < cumulativeProb) {
        selectedSymbol = symbols[i];
        break;
      }
    }
    
    reels.push([selectedSymbol, selectedSymbol, selectedSymbol]);
  }
  
  return reels;
}
```

#### 4.2 당첨금 계산 로직

**파일**: `functions/api/slot/payout-calculator.ts`

```typescript
const SYMBOL_PAYOUTS: Record<string, number> = {
  '⭐': 0.5,
  '🪐': 1,
  '☄️': 2,
  '🚀': 3,
  '👽': 5,
  '💎': 10,
  '👑': 20,
};

interface PayoutResult {
  totalWin: number;
  isJackpot: boolean;
  reelPayouts: number[];
}

export function calculatePayout(
  reelResults: string[][],
  betAmount: number
): PayoutResult {
  // 중앙 라인의 심볼만 체크
  const centerSymbols = reelResults.map(reel => reel[1]); // [1] = 중앙
  
  // 각 릴의 당첨금 계산
  const reelPayouts = centerSymbols.map(symbol => {
    const multiplier = SYMBOL_PAYOUTS[symbol] || 0;
    return betAmount * multiplier;
  });
  
  // 총 당첨금
  const totalWin = reelPayouts.reduce((sum, payout) => sum + payout, 0);
  
  // 잭팟 체크 (3개 모두 동일)
  const isJackpot = centerSymbols.every(s => s === centerSymbols[0]);
  const finalWin = isJackpot ? totalWin * 100 : totalWin;
  
  return { totalWin: finalWin, isJackpot, reelPayouts };
}
```

#### 4.3 슬롯 스핀 API

**파일**: `functions/api/slot/spin.ts`

```typescript
import { Hono } from 'hono';
import { generateServerSeed, hashServerSeed, generateReelResults } from './provably-fair';
import { calculatePayout } from './payout-calculator';

const app = new Hono();

app.post('/api/slot/spin', async (c) => {
  const { walletAddress, betAmount, clientSeed } = await c.req.json();
  
  // 1. 크레딧 확인
  const credit = await c.env.CREDIT_KV.get(`credit:${walletAddress}`);
  if (!credit || parseInt(credit) < betAmount) {
    return c.json({ error: 'Insufficient credit' }, 400);
  }
  
  // 2. 서버 시드 가져오기 또는 생성
  let serverSeed = await c.env.CREDIT_KV.get(`server_seed:${walletAddress}`);
  if (!serverSeed) {
    serverSeed = generateServerSeed();
    await c.env.CREDIT_KV.put(`server_seed:${walletAddress}`, serverSeed);
  }
  
  // 3. 논스 증가
  let nonce = await c.env.CREDIT_KV.get(`nonce:${walletAddress}`);
  const currentNonce = nonce ? parseInt(nonce) + 1 : 1;
  await c.env.CREDIT_KV.put(`nonce:${walletAddress}`, currentNonce.toString());
  
  // 4. Provably Fair 결과 생성
  const reelResults = generateReelResults(serverSeed, clientSeed, currentNonce);
  
  // 5. 당첨금 계산
  const { totalWin, isJackpot } = calculatePayout(reelResults, betAmount);
  
  // 6. 크레딧 업데이트
  const newCredit = parseInt(credit) - betAmount + totalWin;
  await c.env.CREDIT_KV.put(`credit:${walletAddress}`, newCredit.toString());
  
  // 7. 게임 기록 저장
  const gameId = `${Date.now()}_${walletAddress}`;
  await c.env.CREDIT_KV.put(`game:${gameId}`, JSON.stringify({
    walletAddress,
    betAmount,
    reelResults,
    totalWin,
    isJackpot,
    timestamp: Date.now(),
  }), { expirationTtl: 86400 * 30 }); // 30일 보관
  
  // 8. 응답
  return c.json({
    result: reelResults,
    winAmount: totalWin,
    isJackpot,
    serverSeedHash: hashServerSeed(serverSeed),
    nonce: currentNonce,
    gameId, // 더블업용
  });
});

export default app;
```

#### 4.4 더블업 API

**파일**: `functions/api/slot/doubleup.ts`

```typescript
app.post('/api/slot/doubleup', async (c) => {
  const { walletAddress, choice, currentWin, gameId } = await c.req.json();
  
  // 1. 게임 ID 검증 (중복 방지)
  const gameUsed = await c.env.CREDIT_KV.get(`doubleup_used:${gameId}`);
  if (gameUsed) {
    return c.json({ error: 'Already used' }, 400);
  }
  
  // 2. 난수 생성 (0 또는 1, 50% 확률)
  const randomValue = Math.random() < 0.5 ? 0 : 1;
  const userChoice = choice === 'red' ? 0 : 1;
  const success = randomValue === userChoice;
  
  // 3. 결과 계산
  const finalAmount = success ? currentWin * 2 : 0;
  
  // 4. 크레딧 업데이트 (이미 스핀에서 차감됨, 추가만)
  const credit = await c.env.CREDIT_KV.get(`credit:${walletAddress}`);
  const newCredit = parseInt(credit || '0') + finalAmount;
  await c.env.CREDIT_KV.put(`credit:${walletAddress}`, newCredit.toString());
  
  // 5. 게임 ID 무효화
  await c.env.CREDIT_KV.put(`doubleup_used:${gameId}`, 'true', { expirationTtl: 600 });
  
  // 6. 응답
  return c.json({ success, finalAmount });
});
```

---

### Phase 2: 프론트엔드 UI 컴포넌트 (우선순위: 높음)

#### 2.1 슬롯머신 메인 컴포넌트

**파일**: `src/features/slot/components/SlotMachineV2.tsx`

```typescript
import { useState, useEffect } from 'react';
import { Reel } from './Reel';
import { BettingControl } from './BettingControl';
import { DoubleUpModal } from './DoubleUpModal';
import { JackpotVideo } from './JackpotVideo';
import { spinSlot } from '@/api/slot';

interface SlotMachineV2Props {
  walletAddress: string;
  currentCredit: number;
  onCreditChange: () => void;
}

export function SlotMachineV2({ walletAddress, currentCredit, onCreditChange }: SlotMachineV2Props) {
  const [betAmount, setBetAmount] = useState(10);
  const [isSpinning, setIsSpinning] = useState(false);
  const [reelResults, setReelResults] = useState<string[][]>([
    ['⭐', '🪐', '☄️'],
    ['🚀', '👽', '💎'],
    ['👑', '⭐', '🪐'],
  ]);
  const [winAmount, setWinAmount] = useState(0);
  const [isJackpot, setIsJackpot] = useState(false);
  const [showDoubleUp, setShowDoubleUp] = useState(false);
  const [gameId, setGameId] = useState('');

  const handleSpin = async () => {
    if (currentCredit < betAmount) {
      alert('크레딧이 부족합니다!');
      return;
    }

    setIsSpinning(true);
    setWinAmount(0);
    setIsJackpot(false);

    try {
      // 클라이언트 시드 생성
      const clientSeed = crypto.randomUUID();

      // API 호출
      const result = await spinSlot(walletAddress, betAmount, clientSeed);

      // 스핀 애니메이션 (2초)
      await new Promise(resolve => setTimeout(resolve, 2000));

      // 결과 표시
      setReelResults(result.result);
      setWinAmount(result.winAmount);
      setIsJackpot(result.isJackpot);
      setGameId(result.gameId);

      onCreditChange();

      // 잭팟 시 비디오 재생
      if (result.isJackpot) {
        // JackpotVideo 컴포넌트가 처리
      }

      // 더블업 팝업 (당첨금 > 0)
      if (result.winAmount > 0 && !result.isJackpot) {
        setTimeout(() => setShowDoubleUp(true), 3000);
      }
    } catch (error) {
      console.error('Spin failed:', error);
      alert('게임 실행 실패');
    } finally {
      setIsSpinning(false);
    }
  };

  return (
    <div className="slot-machine-v2">
      {/* 릴 디스플레이 */}
      <div className="reels-container">
        {reelResults.map((reel, index) => (
          <Reel key={index} symbols={reel} isSpinning={isSpinning} delay={index * 0.2} />
        ))}
      </div>

      {/* 베팅 컨트롤 */}
      <BettingControl
        betAmount={betAmount}
        onBetChange={setBetAmount}
        maxCredit={currentCredit}
        onSpin={handleSpin}
        isSpinning={isSpinning}
      />

      {/* 당첨 표시 */}
      {winAmount > 0 && (
        <div className="win-display">
          🎉 {winAmount} CSPIN 당첨!
        </div>
      )}

      {/* 잭팟 비디오 */}
      {isJackpot && <JackpotVideo />}

      {/* 더블업 모달 */}
      {showDoubleUp && (
        <DoubleUpModal
          currentWin={winAmount}
          gameId={gameId}
          walletAddress={walletAddress}
          onClose={() => setShowDoubleUp(false)}
          onSuccess={onCreditChange}
        />
      )}
    </div>
  );
}
```

#### 2.2 릴 컴포넌트

**파일**: `src/features/slot/components/Reel.tsx`

```typescript
import { motion } from 'framer-motion';

interface ReelProps {
  symbols: string[];
  isSpinning: boolean;
  delay: number;
}

export function Reel({ symbols, isSpinning, delay }: ReelProps) {
  return (
    <motion.div
      className="reel"
      animate={isSpinning ? { y: [-1000, 0] } : {}}
      transition={{
        duration: 2,
        delay,
        ease: [0.25, 0.1, 0.25, 1], // ease-out cubic
      }}
    >
      {symbols.map((symbol, index) => (
        <div key={index} className="symbol">
          {symbol}
        </div>
      ))}
    </motion.div>
  );
}
```

#### 2.3 베팅 컨트롤

**파일**: `src/features/slot/components/BettingControl.tsx`

```typescript
interface BettingControlProps {
  betAmount: number;
  onBetChange: (amount: number) => void;
  maxCredit: number;
  onSpin: () => void;
  isSpinning: boolean;
}

export function BettingControl({ betAmount, onBetChange, maxCredit, onSpin, isSpinning }: BettingControlProps) {
  const quickBets = [100, 500, 1000];

  return (
    <div className="betting-control">
      {/* 슬라이더 */}
      <input
        type="range"
        min={10}
        max={1000}
        step={10}
        value={betAmount}
        onChange={(e) => onBetChange(parseInt(e.target.value))}
        className="bet-slider"
      />
      
      <div className="bet-amount-display">{betAmount} CSPIN</div>

      {/* 퀵 베팅 버튼 */}
      <div className="quick-bets">
        {quickBets.map(amount => (
          <button key={amount} onClick={() => onBetChange(amount)}>
            {amount}
          </button>
        ))}
        <button onClick={() => onBetChange(maxCredit)}>MAX</button>
      </div>

      {/* 스핀 버튼 */}
      <button
        className="spin-button"
        onClick={onSpin}
        disabled={isSpinning || maxCredit < betAmount}
      >
        {isSpinning ? '⏳ 회전 중...' : '🎰 스핀'}
      </button>
    </div>
  );
}
```

#### 2.4 더블업 모달

**파일**: `src/features/slot/components/DoubleUpModal.tsx`

```typescript
import { useState } from 'react';
import { doubleUp } from '@/api/slot';

interface DoubleUpModalProps {
  currentWin: number;
  gameId: string;
  walletAddress: string;
  onClose: () => void;
  onSuccess: () => void;
}

export function DoubleUpModal({ currentWin, gameId, walletAddress, onClose, onSuccess }: DoubleUpModalProps) {
  const [result, setResult] = useState<'pending' | 'success' | 'fail'>('pending');
  const [finalAmount, setFinalAmount] = useState(0);

  const handleChoice = async (choice: 'red' | 'blue') => {
    try {
      const response = await doubleUp(walletAddress, choice, currentWin, gameId);
      setResult(response.success ? 'success' : 'fail');
      setFinalAmount(response.finalAmount);

      onSuccess();

      // 3초 후 자동 닫힘
      setTimeout(() => onClose(), 3000);
    } catch (error) {
      console.error('DoubleUp failed:', error);
      alert('더블업 실패');
      onClose();
    }
  };

  return (
    <div className="modal-overlay">
      <div className="doubleup-modal">
        {result === 'pending' && (
          <>
            <h2>더블업</h2>
            <p>당첨금을 두 배로 늘릴 기회입니다!</p>
            <div className="current-win">{currentWin} CSPIN</div>

            <div className="choice-buttons">
              <button className="red-button" onClick={() => handleChoice('red')}>
                ❤️ 빨간색
              </button>
              <button className="blue-button" onClick={() => handleChoice('blue')}>
                💙 파란색
              </button>
            </div>

            <button className="skip-button" onClick={onClose}>
              스킵하고 상금 받기
            </button>
          </>
        )}

        {result === 'success' && (
          <div className="result-success">
            <h2>🎉 성공!</h2>
            <p>당첨금이 두 배가 되었습니다!</p>
            <div className="final-amount">{finalAmount} CSPIN</div>
          </div>
        )}

        {result === 'fail' && (
          <div className="result-fail">
            <h2>😢 실패</h2>
            <p>다음 기회를 노려보세요!</p>
            <div className="final-amount">0 CSPIN</div>
          </div>
        )}
      </div>
    </div>
  );
}
```

#### 2.5 잭팟 비디오

**파일**: `src/features/slot/components/JackpotVideo.tsx`

```typescript
import { useEffect, useRef, useState } from 'react';

export function JackpotVideo() {
  const videoRef = useRef<HTMLVideoElement>(null);
  const [isPlaying, setIsPlaying] = useState(true);

  useEffect(() => {
    const video = videoRef.current;
    if (video) {
      video.play();
      video.onended = () => setIsPlaying(false);
    }
  }, []);

  if (!isPlaying) return null;

  return (
    <div className="jackpot-video-overlay">
      <video
        ref={videoRef}
        src="/jackpot_video.mp4"
        autoPlay
        muted={false}
        className="jackpot-video"
      />
    </div>
  );
}
```

---

### Phase 3: 스타일링 및 애니메이션 (우선순위: 중간)

#### 3.1 Tailwind 설정 업데이트

**파일**: `tailwind.config.js`

```javascript
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      colors: {
        // 새로운 슬롯 테마
        slot: {
          primary: '#4A00E0',
          secondary: '#8E2DE2',
          accent: '#FFD700',
          bg: '#0F0F1B',
        },
        rarity: {
          common: '#FFFFFF',
          uncommon: '#4A90E2',
          rare: '#9B59B6',
          legend: '#F1C40F',
        },
      },
      fontFamily: {
        headline: ['Poppins', 'Noto Sans KR', 'sans-serif'],
        body: ['Open Sans', 'Noto Sans KR', 'sans-serif'],
        display: ['Orbitron', 'Courier New', 'monospace'],
      },
      animation: {
        'spin': 'spin 2s cubic-bezier(0.25, 0.1, 0.25, 1)',
        'pulse-win': 'pulse-win 0.5s ease-in-out 3',
        'shake': 'shake 0.5s ease-in-out',
      },
      keyframes: {
        'spin': {
          '0%': { transform: 'translateY(0)' },
          '100%': { transform: 'translateY(-1000px)' },
        },
        'pulse-win': {
          '0%, 100%': { transform: 'scale(1)' },
          '50%': { transform: 'scale(1.1)' },
        },
        'shake': {
          '0%, 100%': { transform: 'translateX(0)' },
          '25%': { transform: 'translateX(-10px)' },
          '75%': { transform: 'translateX(10px)' },
        },
      },
    },
  },
  plugins: [],
}
```

#### 3.2 슬롯머신 스타일

**파일**: `src/features/slot/styles/slot-machine.css`

```css
/* 슬롯머신 컨테이너 */
.slot-machine-v2 {
  background: linear-gradient(135deg, #0F0F1B 0%, #1A1A2E 100%);
  border-radius: 24px;
  padding: 32px;
  box-shadow: 0 20px 60px rgba(0, 0, 0, 0.5);
}

/* 릴 컨테이너 */
.reels-container {
  display: grid;
  grid-template-columns: repeat(3, 120px);
  gap: 16px;
  justify-content: center;
  margin-bottom: 32px;
}

/* 개별 릴 */
.reel {
  width: 120px;
  height: 240px;
  background: rgba(255, 255, 255, 0.05);
  border: 2px solid #4A4A4A;
  border-radius: 12px;
  overflow: hidden;
  display: flex;
  flex-direction: column;
}

/* 심볼 */
.symbol {
  width: 100%;
  height: 80px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 48px;
  border-bottom: 1px solid rgba(255, 255, 255, 0.1);
}

/* 중앙 라인 강조 */
.reel .symbol:nth-child(2) {
  background: rgba(255, 215, 0, 0.1);
  box-shadow: inset 0 0 20px rgba(255, 215, 0, 0.3);
}

/* 당첨 심볼 효과 */
.symbol.win {
  animation: pulse-win 0.5s ease-in-out 3;
  box-shadow: 0 0 25px rgba(255, 215, 0, 0.8);
}

/* 희귀도별 효과 */
.symbol.uncommon {
  filter: drop-shadow(0 0 8px rgba(74, 144, 226, 0.5));
}

.symbol.rare {
  filter: drop-shadow(0 0 15px rgba(155, 89, 182, 0.7));
}

.symbol.legend {
  filter: drop-shadow(0 0 25px rgba(241, 196, 15, 1));
  animation: pulse-win 1s ease-in-out infinite;
}

/* 베팅 슬라이더 */
.bet-slider {
  width: 100%;
  height: 8px;
  background: linear-gradient(to right, #4A90E2, #51CF66);
  border-radius: 4px;
  outline: none;
}

.bet-slider::-webkit-slider-thumb {
  width: 24px;
  height: 24px;
  background: white;
  border-radius: 50%;
  cursor: pointer;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.3);
}

/* 스핀 버튼 */
.spin-button {
  width: 80px;
  height: 80px;
  border-radius: 50%;
  background: linear-gradient(135deg, #4CAF50, #45A049);
  color: white;
  font-size: 24px;
  border: none;
  cursor: pointer;
  transition: all 0.3s ease;
  box-shadow: 0 4px 12px rgba(76, 175, 80, 0.4);
}

.spin-button:hover {
  transform: scale(1.05);
  box-shadow: 0 6px 20px rgba(76, 175, 80, 0.6);
}

.spin-button:active {
  transform: scale(1);
}

.spin-button:disabled {
  background: #9E9E9E;
  cursor: not-allowed;
  box-shadow: none;
}

/* 더블업 모달 */
.modal-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.7);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 1000;
}

.doubleup-modal {
  background: white;
  border-radius: 24px;
  padding: 40px;
  max-width: 500px;
  box-shadow: 0 20px 60px rgba(0, 0, 0, 0.5);
}

.choice-buttons {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 16px;
  margin: 24px 0;
}

.red-button,
.blue-button {
  padding: 20px;
  border-radius: 12px;
  border: none;
  color: white;
  font-size: 20px;
  cursor: pointer;
  transition: all 0.3s ease;
}

.red-button {
  background: linear-gradient(135deg, #FF6B6B, #CC5555);
}

.red-button:hover {
  transform: scale(1.05);
  filter: brightness(1.1);
}

.blue-button {
  background: linear-gradient(135deg, #4A90E2, #357ABD);
}

.blue-button:hover {
  transform: scale(1.05);
  filter: brightness(1.1);
}

/* 잭팟 비디오 */
.jackpot-video-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: black;
  z-index: 9999;
  display: flex;
  align-items: center;
  justify-content: center;
}

.jackpot-video {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

/* 반응형 */
@media (max-width: 768px) {
  .reels-container {
    grid-template-columns: repeat(3, 80px);
    gap: 8px;
  }
  
  .reel {
    width: 80px;
    height: 160px;
  }
  
  .symbol {
    height: 53px;
    font-size: 32px;
  }
  
  .spin-button {
    width: 60px;
    height: 60px;
    font-size: 20px;
  }
}
```

---

## 5. 파일 구조

```
CandleSpinner/
├── functions/
│   └── api/
│       └── slot/
│           ├── spin.ts                 # 슬롯 스핀 API
│           ├── doubleup.ts             # 더블업 API
│           ├── history.ts              # 게임 기록 API
│           ├── provably-fair.ts        # Provably Fair 로직
│           └── payout-calculator.ts    # 당첨금 계산
│
├── src/
│   ├── features/
│   │   └── slot/
│   │       ├── components/
│   │       │   ├── SlotMachineV2.tsx   # 메인 슬롯머신
│   │       │   ├── Reel.tsx            # 릴 컴포넌트
│   │       │   ├── SymbolDisplay.tsx   # 심볼 디스플레이
│   │       │   ├── BettingControl.tsx  # 베팅 컨트롤
│   │       │   ├── DoubleUpModal.tsx   # 더블업 모달
│   │       │   ├── JackpotVideo.tsx    # 잭팟 비디오
│   │       │   └── GameHistory.tsx     # 게임 기록
│   │       ├── hooks/
│   │       │   ├── useSlotGame.ts      # 슬롯 게임 로직
│   │       │   └── useDoubleUp.ts      # 더블업 로직
│   │       ├── styles/
│   │       │   └── slot-machine.css    # 슬롯 스타일
│   │       └── types/
│   │           └── slot.types.ts       # 타입 정의
│   │
│   └── api/
│       └── slot.ts                     # API 클라이언트
│
├── public/
│   └── jackpot_video.mp4               # 잭팟 비디오 (5초)
│
└── docs/
    └── specifications/
        └── 슬롯머신-요구사항-명세서_v1.0.md
```

---

## 6. 개발 일정

| 단계 | 작업 | 기간 | 상태 |
|------|------|------|------|
| **Phase 1** | 백엔드 게임 엔진 | 3일 | 대기 |
| 1.1 | Provably Fair 시스템 | 1일 | - |
| 1.2 | 당첨금 계산 로직 | 0.5일 | - |
| 1.3 | 슬롯 스핀 API | 1일 | - |
| 1.4 | 더블업 API | 0.5일 | - |
| **Phase 2** | 프론트엔드 UI | 4일 | 대기 |
| 2.1 | 슬롯머신 메인 컴포넌트 | 1일 | - |
| 2.2 | 릴 및 심볼 컴포넌트 | 1일 | - |
| 2.3 | 베팅 컨트롤 | 0.5일 | - |
| 2.4 | 더블업 모달 | 1일 | - |
| 2.5 | 잭팟 비디오 | 0.5일 | - |
| **Phase 3** | 스타일링 및 애니메이션 | 2일 | 대기 |
| 3.1 | Tailwind 설정 | 0.5일 | - |
| 3.2 | 슬롯머신 스타일 | 1일 | - |
| 3.3 | 애니메이션 효과 | 0.5일 | - |
| **Phase 4** | 테스트 및 검증 | 3일 | 대기 |
| 4.1 | 확률 분포 검증 (10,000회) | 1일 | - |
| 4.2 | RTP 95% 검증 | 1일 | - |
| 4.3 | 성능 테스트 (1,000 동시) | 1일 | - |
| **Phase 5** | 배포 및 문서화 | 2일 | 대기 |
| 5.1 | 프로덕션 배포 | 1일 | - |
| 5.2 | 사용자 가이드 작성 | 1일 | - |

**총 예상 기간**: 14일

---

## 7. 다음 단계

### 즉시 시작 가능한 작업

1. ✅ **명세서 검토 완료**
2. 🔄 **백엔드 Provably Fair 구현**
3. 🔄 **프론트엔드 기본 UI 구현**

### 필요한 리소스

- [ ] `jackpot_video.mp4` 파일 준비 (5초, 1920x1080)
- [ ] Framer Motion 라이브러리 설치
- [ ] 사운드 효과 파일 (선택)

### 질문 및 확인 사항

1. 잭팟 비디오는 제공되나요? 아니면 제작이 필요한가요?
2. 사운드 효과를 추가하시겠습니까?
3. 게임 기록은 몇 개까지 보관하시겠습니까? (현재: 30일)
4. RTP 통계는 실시간으로 공개하시겠습니까?

---

**문서 끝**
