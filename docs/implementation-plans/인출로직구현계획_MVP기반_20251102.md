# 인출 로직 구현 계획 - TEP-74 표준 기반 (새로 구현)

**작성일**: 2025-11-02  
**상태**: 🔧 구현 대기  
**중요**: MVP 아카이브는 실패 코드. 입금 성공 경험 기반으로 새로 구현

---

## 🎯 목표

**입금 성공 경험을 인출에 적용 (입금의 역방향)**
- TEP-74 표준 정확히 적용
- RPC 방식으로 게임 지갑에서 사용자에게 전송
- destination: 사용자 TON 지갑 (입금의 반대)

---

## 📊 현재 상태 분석

### 입금 (완료) ✅
```
사용자 → 게임
방향: 사용자 Jetton Wallet → 게임 Jetton Wallet
서명: 사용자 (프론트엔드 TON Connect)
destination: GAME_WALLET_ADDRESS (게임의 TON 지갑)
전송 방법: TON Connect sendTransaction()
```

### 인출 (TODO) ❌
```
게임 → 사용자
방향: 게임 Jetton Wallet → 사용자 Jetton Wallet
서명: 게임 (백엔드 RPC)
destination: 사용자의 TON 지갑
전송 방법: TonCenter RPC + BOC 전송
```

### 문제점
```typescript
// src/index.ts handleWithdraw()
async function handleWithdraw(request: Request, env: Env, corsHeaders: Record<string, string>): Promise<Response> {
  // TODO: 실제 Jetton Transfer 구현 필요
  
  // 현재: 크레딧만 차감
  const newCredit = current.credit - body.amount;
  await env.CREDIT_KV.put(key, JSON.stringify({
    credit: newCredit,
    lastUpdated: new Date().toISOString()
  }));
  
  // ❌ 가짜 트랜잭션 해시
  return new Response(JSON.stringify({
    success: true,
    txHash: 'temp_tx_hash',  // ← 문제!
    amount: body.amount,
    credit: newCredit
  }), {
    status: 200,
    headers: { ...corsHeaders, 'Content-Type': 'application/json' },
  });
}
```

**결과**: 팝업에 "temp_tx_hash" 표시 (비정상 동작)

---

## ✅ MVP 아카이브 분석

### 발견된 완전한 인출 로직
```
파일: archive/legacy-code/mvp-v1-backup-20251101/functions/src/handlers/initiate-withdrawal.ts
라인: 1-380
상태: ✅ 완전 구현됨
```

### 핵심 함수

#### 1. buildJettonTransferPayload() (라인 25-43)
```typescript
function buildJettonTransferPayload(
  amount: bigint,
  destination: Address,  // ← 사용자 TON 지갑
  responseTo: Address,   // ← 게임 TON 지갑
  forwardTonAmount: bigint = BigInt(1)
): Cell {
  return beginCell()
    .storeUint(0xf8a7ea5, 32)      // op: jetton transfer
    .storeUint(0, 64)              // query_id
    .storeCoins(amount)            // 인출액
    .storeAddress(destination)     // 사용자 TON 지갑
    .storeAddress(responseTo)      // 게임 TON 지갑
    .storeBit(0)                   // custom_payload (none)
    .storeCoins(forwardTonAmount)  // forward_ton_amount: 1 nanoton
    .storeBit(0)                   // forward_payload (none)
    .endCell();
}
```

**입금과의 차이점**:
| 항목 | 입금 | 인출 |
|------|------|------|
| destination | 게임 TON 지갑 | 사용자 TON 지갑 |
| responseTo | 사용자 TON 지갑 | 게임 TON 지갑 |
| 서명자 | 사용자 (프론트) | 게임 (백엔드) |
| 전송 방법 | TON Connect | RPC (TonCenter) |

#### 2. withdrawViaRpc() (라인 46-148)
```typescript
async function withdrawViaRpc(
  rpc: TonCenterV2Rpc,
  env: any,
  walletAddress: string,         // 사용자 TON 지갑
  withdrawalAmount: number,       // 인출액 (CSPIN)
  userJettonWalletAddress: string // 사용자 Jetton 지갑
): Promise<{ success: boolean; txHash: string; message: string }> {
  
  // 1. 환경변수 확인
  const gameWalletMnemonic = env.GAME_WALLET_PRIVATE_KEY;
  const gameWalletAddress = env.GAME_WALLET_ADDRESS;
  const cspinTokenAddress = env.CSPIN_TOKEN_ADDRESS;
  
  // 2. 니모닉 → 키 쌍 변환
  const { keyPair, wallet: gameWallet } = await getKeyPairAndWallet(gameWalletMnemonic);
  
  // 3. seqno 조회 (원자성 확보)
  const seqnoManager = new SeqnoManager(rpc, env.CREDIT_KV, gameWallet.address.toString());
  const seqno = await seqnoManager.getAndIncrementSeqno();
  
  // 4. TON 잔액 확인
  const tonBalance = await rpc.getBalance(gameWallet.address.toString());
  if (tonBalance < BigInt('50000000')) {  // 0.05 TON
    throw new Error('게임 지갑 TON 부족');
  }
  
  // 5. Jetton Transfer Payload 생성
  const jettonPayload = buildJettonTransferPayload(
    toNano(withdrawalAmount.toString()),
    Address.parse(walletAddress),  // ← 사용자 TON 지갑
    gameWallet.address
  );
  
  // 6. 내부 메시지 생성
  const transferMessage = internal({
    to: Address.parse(userJettonWalletAddress),  // ← 게임의 Jetton 지갑
    value: toNano('0.03'),  // 수수료
    body: jettonPayload
  });
  
  // 7. 트랜잭션 생성 (게임 지갑 서명)
  const transfer = gameWallet.createTransfer({
    seqno,
    secretKey: keyPair.secretKey,  // ← 게임 지갑 개인키
    messages: [transferMessage],
    sendMode: SendMode.PAY_GAS_SEPARATELY | SendMode.IGNORE_ERRORS
  });
  
  // 8. BOC 생성 및 RPC 전송
  const boc = transfer.toBoc().toString('base64');
  const txHash = await rpc.sendBoc(boc);
  
  return {
    success: true,
    txHash,  // ← 실제 트랜잭션 해시!
    message: `RPC 방식 인출 완료: ${withdrawalAmount} CSPIN`
  };
}
```

---

## 🔧 구현 계획

### Phase 1: MVP 코드 복원 (우선순위: 최상)
**목표**: 입금처럼 MVP 아카이브의 검증된 코드 복원

#### 1.1 RPC 유틸리티 복사
```bash
소스: archive/.../functions/src/lib/rpc-utils.ts
대상: functions/src/lib/rpc-utils.ts
```

**포함 기능**:
- `TonCenterV2Rpc` 클래스
- `SeqnoManager` 클래스 (원자성 확보)
- `sendBoc()` 메서드

#### 1.2 니모닉 유틸리티 복사
```bash
소스: archive/.../functions/src/lib/mnemonic-utils.ts
대상: functions/src/lib/mnemonic-utils.ts
```

**포함 기능**:
- `getKeyPairAndWallet()` 함수
- 니모닉 → KeyPair 변환
- WalletContractV5R1 생성

#### 1.3 인출 핸들러 복원
```bash
소스: archive/.../functions/src/handlers/initiate-withdrawal.ts
대상: functions/src/handlers/initiate-withdrawal.ts
```

**포함 기능**:
- `buildJettonTransferPayload()` 함수
- `withdrawViaRpc()` 함수
- 오류 처리 로직

#### 1.4 src/index.ts 수정
```typescript
// ❌ 이전
async function handleWithdraw(...) {
  // TODO: 구현 필요
  return { txHash: 'temp_tx_hash' };
}

// ✅ 수정 후
import { withdrawViaRpc } from './handlers/initiate-withdrawal';

async function handleWithdraw(request: Request, env: Env, corsHeaders: Record<string, string>): Promise<Response> {
  const body = await request.json();
  
  // 1. 크레딧 확인
  const key = `credit:${body.walletAddress}`;
  const current = await env.CREDIT_KV.get(key, 'json') as { credit: number } | null;
  
  if (!current || current.credit < body.amount) {
    return new Response(JSON.stringify({ error: 'Insufficient credit' }), {
      status: 400,
      headers: { ...corsHeaders, 'Content-Type': 'application/json' },
    });
  }
  
  // 2. RPC 인출 실행 (MVP 로직)
  const rpc = new TonCenterV2Rpc(env.TONCENTER_API_KEY);
  const result = await withdrawViaRpc(
    rpc,
    env,
    body.walletAddress,
    body.amount,
    body.userJettonWalletAddress  // ← 프론트엔드에서 전달
  );
  
  // 3. 크레딧 차감 (성공 시)
  const newCredit = current.credit - body.amount;
  await env.CREDIT_KV.put(key, JSON.stringify({
    credit: newCredit,
    lastUpdated: new Date().toISOString()
  }));
  
  return new Response(JSON.stringify({
    success: true,
    txHash: result.txHash,  // ← 실제 트랜잭션 해시!
    amount: body.amount,
    credit: newCredit
  }), {
    status: 200,
    headers: { ...corsHeaders, 'Content-Type': 'application/json' },
  });
}
```

---

### Phase 2: 프론트엔드 수정 (우선순위: 높음)

#### 2.1 Jetton 지갑 주소 계산
**문제**: MVP는 사용자가 직접 입력했지만, 실제로는 자동 계산 필요

**해결 방법** (2가지 옵션):

**옵션 A: 프론트엔드에서 계산** (권장)
```typescript
// src/components/Withdraw.tsx
import { Address } from '@ton/ton';
import { deriveJettonWalletAddress } from '../utils/jetton';

async function handleWithdraw() {
  // 1. 사용자 Jetton 지갑 주소 계산
  const userJettonWallet = await deriveJettonWalletAddress(
    walletAddress,  // 사용자 TON 지갑
    CSPIN_TOKEN_ADDRESS  // Jetton 마스터 주소
  );
  
  // 2. API 요청
  const response = await fetch('/api/withdraw', {
    method: 'POST',
    body: JSON.stringify({
      walletAddress,
      amount: withdrawAmount,
      userJettonWalletAddress: userJettonWallet  // ← 계산된 주소
    })
  });
}
```

**옵션 B: 백엔드에서 계산**
```typescript
// functions/src/handlers/initiate-withdrawal.ts
async function withdrawViaRpc(...) {
  // 사용자 Jetton 지갑 주소 자동 계산
  const userJettonWallet = await deriveJettonWalletAddress(
    walletAddress,
    env.CSPIN_TOKEN_ADDRESS
  );
  
  // 나머지 로직 동일
}
```

**권장**: 옵션 A (프론트엔드)
- 이유: 입금과 일관성 유지
- 입금도 프론트엔드에서 destination 전달

#### 2.2 UI 개선 (옵션)
```tsx
// src/components/Withdraw.tsx
<div>
  <input
    type="number"
    value={withdrawAmount}
    onChange={(e) => setWithdrawAmount(Number(e.target.value))}
    placeholder="인출액 (CSPIN)"
  />
  
  {/* 사용자 Jetton 지갑 주소 표시 (선택) */}
  <p className="text-xs text-gray-500">
    Jetton Wallet: {userJettonWalletAddress?.slice(0, 10)}...
  </p>
  
  <button onClick={handleWithdraw}>
    Withdraw
  </button>
</div>
```

---

### Phase 3: 환경변수 설정 (우선순위: 최상)

#### 3.1 필수 환경변수
```env
# 게임 지갑 (인출 서명용)
GAME_WALLET_PRIVATE_KEY=word1 word2 word3 ...  # 24단어 니모닉
GAME_WALLET_ADDRESS=UQBXs...  # 게임 TON 지갑 주소

# Jetton 정보
CSPIN_TOKEN_ADDRESS=EQD...  # CSPIN Jetton 마스터 주소

# RPC API
TONCENTER_API_KEY=your_api_key_here
```

#### 3.2 설정 위치
1. **로컬 테스트**: `.dev.vars` (Git 미추적)
2. **프로덕션**: Cloudflare Dashboard → Variables

---

### Phase 4: 테스트 (우선순위: 높음)

#### 4.1 단위 테스트
```typescript
// tests/withdraw.test.ts
describe('withdrawViaRpc', () => {
  it('should generate correct Jetton Transfer payload', () => {
    const payload = buildJettonTransferPayload(
      toNano('1'),  // 1 CSPIN
      Address.parse('EQA...'),  // 사용자 TON 지갑
      Address.parse('EQB...')   // 게임 TON 지갑
    );
    
    // payload 검증
    expect(payload).toBeDefined();
  });
  
  it('should throw error if TON balance insufficient', async () => {
    // TON 잔액 부족 시나리오 테스트
  });
});
```

#### 4.2 통합 테스트
```
1. 게임 화면 진입
2. 크레딧 확인 (10 CSPIN 보유)
3. [📤 인출] 버튼 클릭
4. 인출액 입력: 1 CSPIN
5. [Withdraw] 버튼 클릭
6. 로딩 표시 → 성공 팝업
7. 블록체인 익스플로러 확인
8. 크레딧 감소 확인 (9 CSPIN)
```

---

## 🚨 주의사항

### 1. seqno 원자성 (중요!)
```typescript
// SeqnoManager 사용 필수
const seqnoManager = new SeqnoManager(rpc, env.CREDIT_KV, gameWallet.address.toString());
const seqno = await seqnoManager.getAndIncrementSeqno();

// ❌ 직접 조회 금지 (경쟁 조건 발생)
const seqno = await rpc.getSeqno(gameWallet.address.toString());
```

**이유**: 동시 요청 시 seqno 충돌 방지

### 2. 크레딧 차감 타이밍
```typescript
// ✅ 권장: 트랜잭션 성공 후 차감
const result = await withdrawViaRpc(...);
if (result.success) {
  await env.CREDIT_KV.put(key, ...);  // 크레딧 차감
}

// ❌ 비권장: 트랜잭션 전 차감 (실패 시 복구 필요)
await env.CREDIT_KV.put(key, ...);
const result = await withdrawViaRpc(...);
```

### 3. TON 잔액 모니터링
```
게임 지갑 TON 잔액 확인 필요:
- 인출 1회당 약 0.03-0.05 TON 소모
- 잔액 부족 시 인출 불가
- 알림 시스템 구현 권장
```

### 4. 오류 처리
```typescript
try {
  const result = await withdrawViaRpc(...);
} catch (error) {
  // 오류별 처리
  if (error.message.includes('TON 부족')) {
    return { error: '게임 지갑 TON 부족. 관리자에게 문의하세요.' };
  }
  if (error.message.includes('Insufficient credit')) {
    return { error: '크레딧 부족' };
  }
  // 기타 오류
  return { error: '인출 실패. 다시 시도해주세요.' };
}
```

---

## 📈 예상 일정

| Phase | 작업 | 예상 시간 | 우선순위 |
|-------|------|-----------|----------|
| 1 | MVP 코드 복원 | 2-3시간 | 최상 |
| 2 | 프론트엔드 수정 | 1-2시간 | 높음 |
| 3 | 환경변수 설정 | 30분 | 최상 |
| 4 | 테스트 | 1-2시간 | 높음 |
| **총계** | | **4-7시간** | |

---

## ✅ 완료 조건

- [ ] MVP 아카이브 코드 복원 완료
- [ ] `handleWithdraw()` 실제 로직 구현
- [ ] `txHash: 'temp_tx_hash'` 제거
- [ ] 프론트엔드에서 userJettonWalletAddress 전달
- [ ] 환경변수 설정 완료 (로컬 + Cloudflare)
- [ ] 로컬 테스트 성공 (블록체인 트랜잭션 확인)
- [ ] 크레딧 차감 정상 동작
- [ ] 오류 처리 완료

---

## 📚 참고 자료

### MVP 아카이브
- 인출 핸들러: `archive/.../functions/src/handlers/initiate-withdrawal.ts`
- RPC 유틸: `archive/.../functions/src/lib/rpc-utils.ts`
- 니모닉 유틸: `archive/.../functions/src/lib/mnemonic-utils.ts`

### 기존 문서
- Task2: `docs/instructions/Task2-보안인출로직-seqno원자성_20251024_000000.md`
- 인출 버그 수정: `docs/reports/[완성]_인출-버그-수정-최종-보고서_20251024.md`

### 입금 참고
- 입금 성공 경험: `docs/troubleshooting/입금성공경험_MVP분석으로해결_20251102.md`
- 입금 컴포넌트: `src/components/Deposit.tsx`

---

**다음 단계**: Phase 1 (MVP 코드 복원) 시작
